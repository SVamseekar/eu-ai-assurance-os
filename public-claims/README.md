# Public-claims teasers

Canonical packs live next to the API so they ship in the JAR:

`services/api/src/main/resources/public-claims/`

```bash
./scripts/public-claims-evgraph.sh
```

See `docs/PUBLIC_CLAIMS.md` and the public marketing page `/method`. Named organisations are not customers.
