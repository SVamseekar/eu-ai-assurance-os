# Public-claims packs (Evgraph inputs)

Canonical JSON/CSV for `evgraph scan-promotion` and `evgraph scan-dataset-manifest`.

Run from the repo root:

```bash
./scripts/public-claims-evgraph.sh
```

See `docs/PUBLIC_CLAIMS.md`. These named organisations are **not customers**.
